from django.urls import path
# from .views import PostList, PostDetail

app_name = 'blog_api'

urlpatterns = [
    # path('', PostList.as_views(), name='listcreate'),
    # path('<int:pk>/', PostDetail.as_views(), name='detailcreate')
]
